#pragma once
#include "EventSource.h"
#include <stdio.h>
#include <string>

namespace GameEngine {
	/*template<typename P>
	bool EventSource<P>::add(const thisHandler & handler)
	{
		listeners.push_back(handler);
		return true;
	}
	template<typename P>
	bool EventSource<P>::remove(const thisHandler & handler)
	{
		
		return false;
	}*/
	//template<typename P>
	//void EventSource<P>::call(P params)
	//{
	//	printf(std::to_string(params).c_str());
	//	//for each (thisHandler handler in listeners)
	//	//{
	//	//	//SDL_Log("Invoking a handler...");
	//	//	handler(params);
	//	//}
	//}
}